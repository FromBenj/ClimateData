<div class="dropdown d-flex justify-content-end me-2">
    <button class="dropdown-toggle bg-transparent border-0" type="button" id="burgerMenuButton" data-bs-toggle="dropdown" aria-expanded="false">
        <img src="{{ asset('images/burger-menu-icon.svg') }}" alt="Burger menu icon" width="60px">
    </button>
    <div class="dropdown-menu border-2" aria-labelledby="burgerMenuButton">
        <a class="dropdown-item" href="{{ path('temperature_evolution') }}">Temperature Evolution</a>
        <a class="dropdown-item" href="{{ path('temperature_average') }}">Average Temperature Evolution</a>
    </div>
</div>

